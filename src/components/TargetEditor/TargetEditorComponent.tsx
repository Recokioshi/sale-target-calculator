import React from 'react';
import { View } from 'react-native';
import { Text } from 'react-native-elements';

const TargetEditor: React.FC = () => (
  <View>
    <Text>Target Editor</Text>
  </View>
);

export default TargetEditor;
